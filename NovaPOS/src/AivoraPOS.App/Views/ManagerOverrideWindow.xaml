<Window x:Class="AivoraPOS.App.Views.ManagerOverrideWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manager Approval Required"
        Height="300"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Manager Override"
                   FontSize="20"
                   FontWeight="SemiBold" />
        <TextBlock Grid.Row="1"
                   Margin="0,8,0,16"
                   Text="A manager must approve this sensitive action."
                   TextWrapping="Wrap"
                   Foreground="#666" />

        <StackPanel Grid.Row="2">
            <TextBlock Text="Username" Margin="0,0,0,4" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="32" Margin="0,0,0,12" />
            <TextBlock Text="Password" Margin="0,0,0,4" />
            <PasswordBox x:Name="PasswordBox" Height="32" PasswordChanged="OnPasswordChanged" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="#C0392B" Margin="0,12,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="90" Height="32" Margin="0,0,8,0" Command="{Binding CancelCommand}" />
            <Button Content="Approve" Width="90" Height="32" Command="{Binding ApproveCommand}" />
        </StackPanel>
    </Grid>
</Window>
