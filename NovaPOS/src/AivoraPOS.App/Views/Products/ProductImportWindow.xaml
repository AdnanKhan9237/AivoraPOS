<Window x:Class="AivoraPOS.App.Views.Products.ProductImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Products"
        Height="560"
        Width="720"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Import Products (CSV)" FontSize="20" FontWeight="SemiBold" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,16">
            <Button Content="Download Template" Padding="12,6" Margin="0,0,8,0" Command="{Binding DownloadTemplateCommand}" />
            <Button Content="Browse CSV..." Padding="12,6" Command="{Binding BrowseCommand}" />
            <TextBlock Text="{Binding SelectedFilePath}" Margin="12,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Width="320" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding PreviewRows}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding RowNumber}" Width="40" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="100" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="70" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100" />
                <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="60" />
                <DataGridTextColumn Header="Errors" Binding="{Binding ErrorsDisplay}" Width="180" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0,12,0,0" TextWrapping="Wrap" />
        <TextBlock Grid.Row="4" Text="{Binding ImportSummary}" Margin="0,8,0,12" TextWrapping="Wrap" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" Command="{Binding CancelCommand}" />
            <Button Content="Import" Width="90" IsDefault="True" Command="{Binding ImportCommand}" />
        </StackPanel>
    </Grid>
</Window>
