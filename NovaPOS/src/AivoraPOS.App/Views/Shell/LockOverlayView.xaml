<UserControl x:Class="AivoraPOS.App.Views.Shell.LockOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource OverlayBrush}">
        <Border Background="{DynamicResource LockPanelBrush}"
                CornerRadius="8"
                Padding="32"
                MaxWidth="420"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Session Locked"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
                <TextBlock Text="{Binding LockedAtText}"
                           Margin="0,4,0,16"
                           Foreground="{DynamicResource TextSecondaryBrush}" />
                <TextBlock Text="Sign in to resume. Your cart is preserved."
                           TextWrapping="Wrap"
                           Margin="0,0,0,16"
                           Foreground="{DynamicResource TextSecondaryBrush}" />

                <CheckBox Content="Manager / Admin sign-in"
                          IsChecked="{Binding IsManagerLogin}"
                          Margin="0,0,0,16" />

                <StackPanel Visibility="{Binding IsManagerLogin, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Username" Foreground="{DynamicResource TextSecondaryBrush}" />
                    <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="32" Margin="0,4,0,12" />
                    <TextBlock Text="Password" Foreground="{DynamicResource TextSecondaryBrush}" />
                    <PasswordBox x:Name="ManagerPasswordBox" Height="32" Margin="0,4,0,12" PasswordChanged="OnManagerPasswordChanged" />
                </StackPanel>

                <StackPanel>
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsManagerLogin}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Text="Cashier" Foreground="{DynamicResource TextSecondaryBrush}" />
                    <ComboBox ItemsSource="{Binding Cashiers}"
                              DisplayMemberPath="FullName"
                              SelectedItem="{Binding SelectedCashier}"
                              Height="32"
                              Margin="0,4,0,12" />
                    <TextBlock Text="4-digit PIN" Foreground="{DynamicResource TextSecondaryBrush}" />
                    <PasswordBox x:Name="PinPasswordBox" Height="32" MaxLength="4" PasswordChanged="OnPinPasswordChanged" />
                </StackPanel>

                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{DynamicResource DangerBrush}"
                           Margin="0,12,0,0"
                           TextWrapping="Wrap" />

                <Button Content="Unlock"
                        Height="36"
                        Margin="0,16,0,0"
                        Command="{Binding UnlockCommand}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
