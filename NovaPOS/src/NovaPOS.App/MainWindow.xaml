<Window x:Class="NovaPOS.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sales="clr-namespace:NovaPOS.App.Views.Sales"
        mc:Ignorable="d"
        Title="NovaPOS"
        Height="800"
        Width="1360"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1F4E79" Padding="12,8">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="Audit Log" Padding="12,4" Margin="0,0,8,0"
                            Visibility="{Binding CanViewAuditLog, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Command="{Binding OpenAuditLogCommand}" />
                    <Button Content="Sign Out" Padding="12,4" Margin="0,0,8,0" Command="{Binding SignOutCommand}" />
                    <Button Content="Activate License" Padding="12,4" Command="{Binding ActivateLicenseCommand}" />
                </StackPanel>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding LicenseStatusText}" Foreground="White" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding UserStatusText}" Foreground="#D6E4F0" Margin="0,2,0,0" FontSize="12" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Background="#FFF4CE" Padding="12,6"
                Visibility="{Binding ShowTrialBanner, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="Trial mode is active. Upgrade to remove product limits and receipt watermarks." Foreground="#7A5B00" />
        </Border>

        <sales:SalesScreen Grid.Row="2" Margin="12" DataContext="{Binding Sales}" />
    </Grid>
</Window>
