<Window x:Class="NovaPOS.App.Views.Sales.DiscountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Apply Discount"
        Height="480"
        Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        KeyDown="OnKeyDown">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Discounts" FontSize="20" FontWeight="SemiBold" />
        <StackPanel Grid.Row="1" Margin="0,12,0,12">
            <TextBlock Text="Order discount ($)" />
            <TextBox Text="{Binding OrderDiscountAmount, UpdateSourceTrigger=PropertyChanged}" Height="32" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding LineDiscounts}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item" Binding="{Binding ProductName}" Width="*" IsReadOnly="True" />
                <DataGridTextColumn Header="Discount $" Binding="{Binding DiscountAmount, UpdateSourceTrigger=PropertyChanged}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="OnCancel" />
            <Button Content="Apply" Width="90" Click="OnApply" />
        </StackPanel>
    </Grid>
</Window>
