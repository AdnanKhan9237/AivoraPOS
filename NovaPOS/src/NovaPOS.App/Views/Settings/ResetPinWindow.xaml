<Window x:Class="NovaPOS.App.Views.Settings.ResetPinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reset PIN"
        Height="200"
        Width="360"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}">
    <StackPanel Margin="24">
        <TextBlock Text="{Binding Prompt}" TextWrapping="Wrap" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12" />
        <PasswordBox x:Name="PinBox" MaxLength="4" Height="32" PasswordChanged="OnPinChanged" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="OK" Width="90" IsDefault="True" Click="OnOk" />
        </StackPanel>
    </StackPanel>
</Window>
